The code must currently be in a file called 'code.subleq'

Comments:
    Every word that isn't a number (any digit, - and .) is a comment

Special features:
    Exiting:
        Jump to address -1, -2 is the exit code
    Printing:
        Jump to address -3 to print the number in -5 to stdout
        Jump to address -4 to print the number in -5 to stderr